export class Order {
    constructor(
        public materialNumber : string, 
        public quantityRequired: number, 
        public peinNumber: string,
        public shelf: string,
        ) { }
}
